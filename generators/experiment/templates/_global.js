$('body').addClass('<%= name %>');
window['<%= name %>'] = function(options){
  options = options || {};
  var variation = options.variation || {};
  //Suggestd: start logic here
  switch(variation){
    case :

    break;
    default:
      break;
  };
}
